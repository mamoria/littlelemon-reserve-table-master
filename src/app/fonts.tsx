import { Inter, Karla, Markazi_Text } from 'next/font/google'

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const karla = Karla({
  subsets: ["latin"],
  display: "swap",
});

export const markazi = Markazi_Text({
  subsets: ["latin"],
  display: "swap",
});